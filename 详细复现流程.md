 Evo-RL / RECAP 复现操作流程

> 基于双臂 SO101 + pi05 + 单卡 A100 80GB
> 任务：`bi_so101_handover_cube` — 左臂抓方块递给右臂

---

## 总览：9 步闭环

```text
Step 0  本地硬件联调
Step 1  填好 env.sh 中的相机路径
Step 2  采集纯人工 demo (30 条)
Step 3  A100 训练初始 pi05 SFT
Step 4  本地 human-in-loop rollout (20 条 round1)
Step 5  A100 合并 demo + round1
Step 6  A100 训练 value model
Step 7  A100 做 value inference，写回 indicator
Step 8  A100 做 ACP policy 训练
Step 9  本地部署 ACP policy，开始 round2
```

---

## Step 0：初始化环境变量

**每次开新终端，先执行一次：**

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
```

执行后立刻验证：

```bash
echo "TASK_NAME     = $TASK_NAME"
echo "DEMO_REPO_ID  = $DEMO_REPO_ID"
echo "ROUND1_REPO_ID= $ROUND1_REPO_ID"
echo "LOCAL_DATA_ROOT= $LOCAL_DATA_ROOT"
echo "LEFT_FOLLOWER  = $LEFT_FOLLOWER_PORT"
echo "RIGHT_FOLLOWER = $RIGHT_FOLLOWER_PORT"
echo "LEFT_LEADER    = $LEFT_LEADER_PORT"
echo "RIGHT_LEADER   = $RIGHT_LEADER_PORT"
```

期望看到：

```text
TASK_NAME      = bi_so101_handover_cube
DEMO_REPO_ID   = local/bi_so101_handover_cube_demo
ROUND1_REPO_ID = local/eval_bi_so101_handover_cube_round1
LOCAL_DATA_ROOT = /home/jy/Data/YCP/lerobot_datasets
LEFT_FOLLOWER   = /dev/serial/by-id/usb-1a86_USB_Single_Serial_58FA082672-if00
RIGHT_FOLLOWER  = /dev/serial/by-id/usb-1a86_USB_Single_Serial_58FA083353-if00
LEFT_LEADER     = /dev/serial/by-id/usb-1a86_USB_Single_Serial_5B14029908-if00
RIGHT_LEADER    = /dev/serial/by-id/usb-1a86_USB_Single_Serial_5B14113707-if00
```



如果任何一行为空或路径不对，先修 `env.sh` 再继续。

---

## Step 1：确认相机路径并补到 env.sh

### 1.1 列出所有 V4L2 设备

```bash
v4l2-ctl --list-devices
```

### 1.2 找到稳定路径

**不要用 `/dev/video0` 这种编号路径**，因为每次插拔顺序可能变。

请使用 `by-path` 路径：

```bash
ls -la /dev/v4l/by-path/
```

你会看到类似：

```text
pci-0000:00:14.0-usb-0:3:1.0-video-index0 -> ../../video2
pci-0000:00:14.0-usb-0:4:1.0-video-index0 -> ../../video4
pci-0000:00:14.0-usb-0:1:1.0-video-index0 -> ../../video0
```

### 1.3 逐个确认哪个路径是哪个相机

对每个 `by-path` 路径，用 ffmpeg 抓一帧看是不是彩色画面：

```bash
# 替换 <PATH> 为上一步列出的 by-path 路径
ffmpeg -hide_banner -loglevel error -y \
  -f v4l2 -video_size 640x480 -i /dev/v4l/by-path/<PATH> \
  -frames:v 1 /tmp/test_cam.jpg

# 查看图片（如果有桌面环境）
xdg-open /tmp/test_cam.jpg
# 或者复制到本地查看
```

逐个试，确认哪个是左腕、右腕、前视。

> **RealSense D435 注意**：D435 通过 V4L2 会出现多个节点，只有一个是彩色（YUYV/MJPG），其余是深度和红外。先用 `v4l2-ctl -d /dev/v4l/by-path/<PATH> --list-formats-ext` 确认格式为 YUYV 或 MJPG 的那个是彩色口。

### 1.4 更新 env.sh

确认后，把 `env.sh` 里的占位符替换成真实路径：

```bash
# env.sh 中需要修改的三行（替换为你实际确认的 by-path）
export LEFT_WRIST_CAM="/dev/v4l/by-path/你的左腕相机路径"
export RIGHT_WRIST_CAM="/dev/v4l/by-path/你的右腕相机路径"
export FRONT_CAM="/dev/v4l/by-path/你的前视相机路径"
```

修改后重新 `source env.sh`。

---

## 双臂硬件建议与校准文件（跑 teleop / record 前必读）

对于双臂使用者，我们建议**左侧从臂（follower）与左侧主臂（leader）**上、与各伺服电机对应的连杆与末端结构（官方文档常强调关节 **4 / 5 / 6** 一带）尽量**对称或镜像安装**，这样双手遥操作时左右手感更一致，通常更自然。

在运行任何双臂相关命令（`bi_so_follower` / `bi_so_leader`）之前，请确认校准文件已存在。默认路径为：

`~/.cache/huggingface/lerobot/calibration/`

目录结构应类似（文件名需与你在命令里使用的 `--robot.id`、`--teleop.id` 及左右臂约定一致；本仓库 `env.sh` 中示例 id 对应如下）：

```text
calibration/
├── robots
│   └── so_follower
│       ├── bi_so101_follower_left.json
│       └── bi_so101_follower_right.json
└── teleoperators
    └── so_leader
        ├── bi_so101_leader_left.json
        └── bi_so101_leader_right.json
```

若你改用了别的 `ROBOT_ID` / `TELEOP_ID`，校准 JSON 的命名需与 LeRobot 双臂校准流程生成的文件名一致。缺失时请先按 [SO 系列官方教程](https://wiki.seeedstudio.com/cn/lerobot_so100m/) 完成安装与校准，再进入 Step 2。

---

## Step 2：本地硬件联调（teleop 测试）

### 2.1 激活环境

```bash
conda activate lerobot
source env.sh
```

### 2.2 运行 teleop

```bash
lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id=${ROBOT_ID} \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.id=${TELEOP_ID} \
  --display_data=true
```
```bash
lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id=${ROBOT_ID} \
```
### 2.3 通过标准

- [ ] 左臂 follower 能跟随 leader
- [ ] 右臂 follower 能跟随 leader
- [ ] 三个相机画面稳定（不黑屏、不花屏）
- [ ] 手动完成一次"左臂抓 → 递给右臂"的完整动作
- [ ] 没有明显串口掉线、机械臂抽搐、相机掉帧

**这一步不过，不要往下走。**

---

## Step 3：采集纯人工 demo（30 条）

### 3.1 创建数据目录

```bash
mkdir -p ${LOCAL_DATA_ROOT}
```

### 3.2 执行采集

```bash
lerobot-record \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id=${ROBOT_ID} \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.id=${TELEOP_ID} \
  --dataset.repo_id=${DEMO_REPO_ID} \
  --dataset.root=${LOCAL_DATA_ROOT}/${DEMO_REPO_ID} \
  --dataset.single_task="${TASK_TEXT}" \
  --dataset.num_episodes=30 \
  --dataset.episode_time_s=60 \
  --dataset.reset_time_s=30 \
  --dataset.push_to_hub=false \
  --display_data=true
```

### 3.3 采集要点

- task 文本已固定为 `"Pick up the cube with the left arm and hand it to the right arm."`，不要改
- 尽量每条风格一致
- 成功轨迹优先，少收明显失败样本
- 如果中断想继续追加，加 `--resume=true`（注意 `num_episodes` 是追加数量）

### 3.4 采完检查

```bash
lerobot-dataset-report --dataset ${LOCAL_DATA_ROOT}/${DEMO_REPO_ID}
```

确认：

- [ ] episode 数量 ≥ 30
- [ ] task 文本只有一句
- [ ] 图像、状态、动作字段不缺失
- [ ] 平均长度合理

---

## Step 4：A100 上训练初始 pi05 SFT

### 4.1 上传 demo 数据到 A100

```bash
rsync -avz ${LOCAL_DATA_ROOT}/${DEMO_REPO_ID}/ \
  <A100_USER>@<A100_HOST>:${SERVER_DATA_ROOT}/${DEMO_REPO_ID}/
```

> 将 `<A100_USER>@<A100_HOST>` 替换为你的服务器地址。

### 4.2 在 A100 上初始化环境

```bash
# SSH 登录 A100 后
conda activate lerobot    # 或 conda activate /llm_jzm/cache/conda_env/lerobot
source env.sh

# 如果是共享服务器，还需要：
export HF_HOME=/llm_jzm/cache/huggingface/
export HF_TOKEN=<SHARED_HF_TOKEN>
export HF_ENDPOINT=https://hf-mirror.com
export WANDB_API_KEY=<SHARED_WANDB_API_KEY>
wandb login --relogin "$WANDB_API_KEY"
```

### 4.3 训练

```bash
lerobot-train \
  --dataset.repo_id=${DEMO_REPO_ID} \
  --dataset.root=${SERVER_DATA_ROOT}/${DEMO_REPO_ID} \
  --policy.type=pi05 \
  --policy.pretrained_path=lerobot/pi05_base \
  --policy.device=cuda \
  --policy.dtype=bfloat16 \
  --policy.gradient_checkpointing=true \
  --policy.compile_model=false \
  --policy.freeze_vision_encoder=false \
  --policy.train_expert_only=false \
  --batch_size=8 \
  --steps=10000 \
  --acp.enable=false \
  --output_dir=outputs/train/${PI05_SFT_R0_NAME} \
  --job_name=${PI05_SFT_R0_NAME} \
  --wandb.enable=true \
  --policy.push_to_hub=false
```

### 4.4 OOM 应急

```bash
# 方法1：降 batch
--batch_size=4

# 方法2：先只训 expert 层
--policy.train_expert_only=true
```

### 4.5 通过标准

- [ ] 训练完整结束，无报错
- [ ] `outputs/train/${PI05_SFT_R0_NAME}/checkpoints/last/pretrained_model/` 存在
- [ ] wandb 上 loss 有下降趋势

### 4.6 把 policy 拷回本地机

```bash
# 在本地机执行
mkdir -p ~/Data/policies
rsync -avz <A100_USER>@<A100_HOST>:~/Data/YCP/Evo-RL/outputs/train/${PI05_SFT_R0_NAME}/checkpoints/last/pretrained_model/ \
  ~/Data/policies/${PI05_SFT_R0_NAME}/
```

---

## Step 5：本地 human-in-loop rollout（20 条 round1）

### 5.1 热键速记

| 按键 | 功能 |
|------|------|
| `i` | 进入/退出人工接管 |
| `s` | 标记成功并结束当前 episode |
| `f` | 标记失败并结束当前 episode |
| `→` | 提前结束当前循环 |
| `←` | 提前结束并重录当前 episode |
| `Esc` | 结束整个录制会话 |

### 5.2 执行采集

```bash
conda activate lerobot
source env.sh

lerobot-human-inloop-record \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id=${ROBOT_ID} \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.id=${TELEOP_ID} \
  --dataset.repo_id=${ROUND1_REPO_ID} \
  --dataset.root=${LOCAL_DATA_ROOT}/${ROUND1_REPO_ID} \
  --dataset.single_task="${TASK_TEXT}" \
  --dataset.num_episodes=20 \
  --dataset.episode_time_s=60 \
  --dataset.reset_time_s=30 \
  --dataset.push_to_hub=false \
  --display_data=true \
  --policy.path=~/Data/policies/${PI05_SFT_R0_NAME}
```

### 5.3 操作策略

- 先让 policy 自己做
- 明显要失败时按 `i` 接管纠正
- 纠正完按 `i` 交还给 policy
- 每条 episode 认真按 `s`（成功）或 `f`（失败）
- **不要全程替 policy 做完** — 否则和普通 demo 没区别

### 5.4 采完检查

```bash
lerobot-dataset-report --dataset ${LOCAL_DATA_ROOT}/${ROUND1_REPO_ID}
```

确认：

- [ ] episode 数量正确
- [ ] 成功/失败数量合理（不是全成功也不是全失败）
- [ ] schema 包含 `complementary_info.policy_action`
- [ ] schema 包含 `complementary_info.is_intervention`
- [ ] schema 包含 `complementary_info.state`
- [ ] schema 包含 `complementary_info.collector_policy_id`

**这些字段缺任何一个，不要进下一步。**

---

## Step 6：A100 合并 demo + round1

### 6.1 上传 round1 到 A100

```bash
rsync -avz ${LOCAL_DATA_ROOT}/${ROUND1_REPO_ID}/ \
  <A100_USER>@<A100_HOST>:${SERVER_DATA_ROOT}/${ROUND1_REPO_ID}/
```

### 6.2 在 A100 上 merge

```bash
source env.sh

lerobot-edit-dataset \
  --repo_id=${MERGED_R1_REPO_ID} \
  --root=${SERVER_DATA_ROOT} \
  --operation.type=merge \
  --operation.repo_ids="['${DEMO_REPO_ID}','${ROUND1_REPO_ID}']"
```

### 6.3 检查 merge 结果

```bash
lerobot-dataset-report --dataset ${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID}
```

确认：

- [ ] 总 episode = demo 条数 + round1 条数
- [ ] task 文本统一
- [ ] intervention 字段还在
- [ ] episode_success 字段还在

---

## Step 7：A100 训练 value model

```bash
source env.sh

lerobot-value-train \
  --dataset.repo_id=${MERGED_R1_REPO_ID} \
  --dataset.root=${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID} \
  --value.type=pistar06 \
  --value.dtype=bfloat16 \
  --targets.success_field=episode_success \
  --targets.default_success=failure \
  --targets.c_fail_coef=1.0 \
  --batch_size=64 \
  --steps=8000 \
  --output_dir=outputs/value_train/${TASK_NAME}_r1 \
  --job_name=${VALUE_R1_NAME} \
  --wandb.enable=true \
  --value.push_to_hub=false
```

OOM 时降到 `--batch_size=32`。

### 通过标准

- [ ] 训练正常结束
- [ ] `outputs/value_train/${TASK_NAME}_r1` 下有 checkpoint

---

## Step 8：A100 做 value inference，写回 indicator

```bash
source env.sh

lerobot-value-infer \
  --dataset.repo_id=${MERGED_R1_REPO_ID} \
  --dataset.root=${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID} \
  --inference.checkpoint_path=outputs/value_train/${TASK_NAME}_r1 \
  --inference.checkpoint_ref=last \
  --runtime.device=cuda \
  --runtime.batch_size=128 \
  --acp.enable=true \
  --acp.n_step=50 \
  --acp.positive_ratio=0.3 \
  --acp.c_fail_coef=1.0 \
  --a