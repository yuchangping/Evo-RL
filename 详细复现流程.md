# Evo-RL / RECAP 详细复现流程

> 适用对象：当前这套本地修改过的双臂 SO101 系统
> 硬件形态：双臂、7 自由度、7 个舵机 / 每臂
> 任务：`bi_so101_handover_cube`
> 任务文本：`Pick up the cube with the left arm and hand it to the right arm.`
> 当前工作流：本地机采集 + A100 单卡训练 + 再回传本地部署

---

## 0. 文档目标

这份文档不是概念介绍，而是给你这套已经改过代码的系统，整理出一份可以直接照着执行的复现手册。

本手册综合了下面几部分内容：

- 你提供的 `EVO-RL复现.docx`
- 当前仓库里的 [README.md](/home/jy/Data/YCP/Evo-RL/README.md)
- 当前仓库里的 [A100服务器训练RECAP详细流程 copy.md](/home/jy/Data/YCP/Evo-RL/A100服务器训练RECAP详细流程%20copy.md)
- 当前仓库里的 [env.sh](/home/jy/Data/YCP/Evo-RL/env.sh)
- 你这次联调过程中已经确认过的双臂、校准、夹爪映射、三相机配置

说明：

- 你之前给的飞书链接当前环境无法直接读取正文，所以本文档前半部分是按你提供的 `EVO-RL复现.docx` 和仓库现有流程整理的。
- 本文档优先保证“能执行、能复现、和你当前代码一致”。

---

## 1. 当前这套代码的关键约定

你现在这套代码已经不是原始 6 自由度版本，而是已经改成了 7 自由度版本，文档和命令都按下面这个约定来：

### 1.1 关节与 ID 约定

- 每条手臂 7 个舵机，7 个自由度
- 新增自由度是前臂旋转关节 `forearm_roll`
- 当前关节顺序为：

```text
shoulder_pan
shoulder_lift
elbow_flex
forearm_roll
wrist_flex
wrist_roll
gripper
```

- 左臂舵机 ID：`1-7`
- 右臂舵机 ID：`8-14`

也就是说：

- 左从臂搜索 ID：`1-7`
- 右从臂搜索 ID：`8-14`
- 左主臂搜索 ID：`1-7`
- 右主臂搜索 ID：`8-14`

### 1.2 当前 env.sh 约定

当前 [env.sh](/home/jy/Data/YCP/Evo-RL/env.sh) 里已经固定了下面这些值：

```bash
export DATA_NS=local
export TASK_NAME=bi_so101_handover_cube
export TASK_TEXT="Pick up the cube with the left arm and hand it to the right arm."

export LOCAL_DATA_ROOT=/home/jy/Data/YCP/lerobot_datasets
export SERVER_DATA_ROOT=/home/jy/Data/a100_lerobot_datasets

export DEMO_REPO_ID=${DATA_NS}/${TASK_NAME}_demo
export ROUND1_REPO_ID=${DATA_NS}/eval_${TASK_NAME}_round1
export MERGED_R1_REPO_ID=${DATA_NS}/${TASK_NAME}_merged_r1

export PI05_SFT_R0_NAME=${TASK_NAME}_pi05_sft_r0
export VALUE_R1_NAME=${TASK_NAME}_value_r1
export PI05_ACP_R1_NAME=${TASK_NAME}_pi05_acp_r1

export ROBOT_ID=bi_follower_arm
export TELEOP_ID=bi_leader_arm
```

### 1.3 当前串口映射

当前 [env.sh](/home/jy/Data/YCP/Evo-RL/env.sh) 中已经确认的串口是：

```bash
export LEFT_FOLLOWER_PORT="/dev/serial/by-id/usb-1a86_USB_Single_Serial_58FA083353-if00"
export RIGHT_FOLLOWER_PORT="/dev/serial/by-id/usb-1a86_USB_Single_Serial_5B14031881-if00"
export LEFT_LEADER_PORT="/dev/serial/by-id/usb-1a86_USB_Single_Serial_58FA082672-if00"
export RIGHT_LEADER_PORT="/dev/serial/by-id/usb-1a86_USB_Single_Serial_5B14029908-if00"
```

### 1.4 当前相机映射

当前 [env.sh](/home/jy/Data/YCP/Evo-RL/env.sh) 中已经确认的相机路径是：

```bash
export LEFT_WRIST_CAM="/dev/v4l/by-path/pci-0000:00:14.0-usb-0:9.2:1.0-video-index0"
export RIGHT_WRIST_CAM="/dev/v4l/by-path/pci-0000:00:14.0-usb-0:9.3:1.0-video-index0"
export FRONT_CAM="/dev/v4l/by-path/pci-0000:00:14.0-usb-0:9.1:1.3-video-index0"
```

注意：

- 左腕相机挂在 `LEFT_CAM_CFG`
- 右腕相机和前视相机都挂在 `RIGHT_CAM_CFG`
- 这是当前双臂相机配置的正常用法，不是写错

### 1.5 当前夹爪映射

为了让主臂夹爪较短的行程就能驱动从臂夹爪完整开合，当前遥操作命令统一使用：

```bash
--teleop.left_arm_config.gripper_input_min=0
--teleop.left_arm_config.gripper_input_max=40
--teleop.right_arm_config.gripper_input_min=0
--teleop.right_arm_config.gripper_input_max=40
```

### 1.6 当前双臂校准文件命名

因为当前 `ROBOT_ID` 和 `TELEOP_ID` 分别是：

- `ROBOT_ID=bi_follower_arm`
- `TELEOP_ID=bi_leader_arm`

所以双臂校准文件应该是下面这四个：

```text
~/.cache/huggingface/lerobot/calibration/robots/so_follower/bi_follower_arm_left.json
~/.cache/huggingface/lerobot/calibration/robots/so_follower/bi_follower_arm_right.json
~/.cache/huggingface/lerobot/calibration/teleoperators/so_leader/bi_leader_arm_left.json
~/.cache/huggingface/lerobot/calibration/teleoperators/so_leader/bi_leader_arm_right.json
```

---

## 2. 总体闭环

这套 Evo-RL / RECAP 风格流程，本质上是下面这个迭代闭环：

```text
Step 0  本地环境初始化
Step 1  串口 / 相机 / 校准确认
Step 2  双臂遥操作 + 三相机联调
Step 3  采集纯人工 demo（30 条）
Step 4  A100 训练初始 pi05 SFT
Step 5  本地 human-in-loop rollout（20 条 round1）
Step 6  A100 合并 demo + round1
Step 7  A100 训练 value model
Step 8  A100 做 value inference，写回 ACP indicator
Step 9  A100 训练 ACP policy
Step 10 本地部署 ACP policy，进入 round2
```

可以把它理解成：

```text
纯人工示范数据
    ->
初始 policy
    ->
human-in-loop 新一轮数据
    ->
训练 value model
    ->
对数据逐帧打 value / advantage / indicator
    ->
用 indicator 训练 advantage-conditioned policy
    ->
把新 policy 再部署回机器人
```

---

## Step 0：每次开新终端先做的事

### 0.0 如果你在本地 5060Ti 机器上用 Cursor AppImage

如果你当前不是直接用系统安装版 Cursor，而是用 `AppImage`，可以按下面方式启动：

```bash
chmod +x ./Cursor-2.3.35-x86_64.AppImage
./Cursor-2.3.35-x86_64.AppImage --appimage-extract
cd ~/Cursor/squashfs-root
./AppRun --no-sandbox
```

这一段不影响机械臂流程本身，只是本地 5060Ti 开发机上常见的启动方式。

### 0.1 进入仓库并激活环境

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
```

如果 A100 服务器上的 conda 环境不叫 `evo-rl`，就把上面第二行替换成你服务器实际的环境名。

### 0.2 检查变量是否已经加载

```bash
echo "TASK_NAME        = $TASK_NAME"
echo "TASK_TEXT        = $TASK_TEXT"
echo "DEMO_REPO_ID     = $DEMO_REPO_ID"
echo "ROUND1_REPO_ID   = $ROUND1_REPO_ID"
echo "MERGED_R1_REPO_ID= $MERGED_R1_REPO_ID"
echo "LEFT_FOLLOWER    = $LEFT_FOLLOWER_PORT"
echo "RIGHT_FOLLOWER   = $RIGHT_FOLLOWER_PORT"
echo "LEFT_LEADER      = $LEFT_LEADER_PORT"
echo "RIGHT_LEADER     = $RIGHT_LEADER_PORT"
echo "LEFT_WRIST_CAM   = $LEFT_WRIST_CAM"
echo "RIGHT_WRIST_CAM  = $RIGHT_WRIST_CAM"
echo "FRONT_CAM        = $FRONT_CAM"
echo "ROBOT_ID         = $ROBOT_ID"
echo "TELEOP_ID        = $TELEOP_ID"
```

如果有任何一项为空，先修 [env.sh](/home/jy/Data/YCP/Evo-RL/env.sh)，不要继续往下跑。

---

## Step 1：串口、相机、校准确认

这一节对应你 `EVO-RL复现.docx` 前半部分的内容，先把底层硬件状态确认干净。

### 1.1 查看串口

```bash
ls /dev/ttyACM*
ls -la /dev/serial/by-id/
lerobot-find-port
```

### 1.2 给串口临时开放权限

```bash
sudo chmod 666 /dev/ttyACM*
```

如果你的设备不是 `ttyACM*`，而是别的串口类型，请按实际设备名改。

### 1.3 飞特调试工具里的最后检查

在飞特调试工具里，建议先逐一检查每个舵机：

- 每个舵机的位置上下限都合理
- 不要出现负数位置
- 不要出现超过 `4096` 的位置
- 最后把每个舵机调回 `2047` 中位

做完这一步，再继续下面的端口、校准和遥操作命令，会更稳。

### 1.4 查看相机设备

```bash
ls -l /dev/video*
ls -l /dev/v4l/by-path
v4l2-ctl --list-devices
```

### 1.5 检查当前三路相机是否支持目标格式

```bash
v4l2-ctl -d "$LEFT_WRIST_CAM" --list-formats-ext
v4l2-ctl -d "$RIGHT_WRIST_CAM" --list-formats-ext
v4l2-ctl -d "$FRONT_CAM" --list-formats-ext
```

只要能看到 `640x480` 且存在 `30 fps` 的格式，就可以继续。

### 1.6 如果还没校准，先做双臂校准

如果你已经校准完成，可以直接跳到 Step 2。

#### 双臂 follower 校准

```bash
source env.sh

lerobot-calibrate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}"
```

#### 双臂 leader 校准

```bash
source env.sh

lerobot-calibrate \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.id="${TELEOP_ID}"
```

说明：

- 当前代码已经改成 7 自由度校准，会走完整 7 个关节
- 左臂按 `1-7` 搜索
- 右臂按 `8-14` 搜索
- 如果你看到 `wrist_roll` 没有出现在范围采集提示里，不一定是串口问题
- 这是因为 `so101_leader` 的某些校准逻辑会把 `wrist_roll` 当作全圈关节处理

#### 如果你要单独校准单臂

左 follower：

```bash
lerobot-calibrate \
  --robot.type=so100_follower \
  --robot.port="${LEFT_FOLLOWER_PORT}" \
  --robot.id=left_follower_arm
```

右 follower：

```bash
lerobot-calibrate \
  --robot.type=so100_follower \
  --robot.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id=right_follower_arm
```

左 leader：

```bash
lerobot-calibrate \
  --teleop.type=so100_leader \
  --teleop.port="${LEFT_LEADER_PORT}" \
  --teleop.id=left_leader_arm
```

右 leader：

```bash
lerobot-calibrate \
  --teleop.type=so100_leader \
  --teleop.port="${RIGHT_LEADER_PORT}" \
  --teleop.id=right_leader_arm
```

### 1.7 检查校准文件是否存在

```bash
ls ~/.cache/huggingface/lerobot/calibration/robots/so_follower/
ls ~/.cache/huggingface/lerobot/calibration/teleoperators/so_leader/
```

双臂情况下，你至少应该看到：

```text
bi_follower_arm_left.json
bi_follower_arm_right.json
bi_leader_arm_left.json
bi_leader_arm_right.json
```

---

## Step 2：双臂遥操作与三相机联调

这一节先确认机械臂能动，再确认相机能看，最后把两者合起来。

### 2.1 不带相机，先测双臂遥操作

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*

lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}"
```

通过标准：

- 左 leader 能稳定控制左 follower
- 右 leader 能稳定控制右 follower
- 夹爪能正常开合
- 手动能完成一次“左抓取 -> 递给右臂”

### 2.2 单独分别测试三路相机

左腕相机：

```bash
ffplay -f v4l2 -input_format mjpeg -framerate 30 -video_size 640x480 "$LEFT_WRIST_CAM"
```

右腕相机：

```bash
ffplay -f v4l2 -input_format mjpeg -framerate 30 -video_size 640x480 "$RIGHT_WRIST_CAM"
```

前视相机：

```bash
ffplay -f v4l2 -input_format mjpeg -framerate 30 -video_size 640x480 "$FRONT_CAM"
```

通过标准：

- 三路都能单独稳定出图
- 没有黑屏、花屏、卡死

### 2.3 带三路相机再测双臂遥操作

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*

lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}" \
  --display_data=true
```

通过标准：

- 左右双臂都能正常遥操作
- 左腕、右腕、前视三路画面都能显示
- 没有串口掉线
- 没有明显抽搐
- 没有持续性的严重掉帧

提示：

- 如果出现 `failed to set fourcc=MJPG` 但画面正常，这只是警告，可以继续
- 如果出现驱动回报 FPS 和设置不一致，现在代码会给告警，不应该再直接退出

---

## Step 3：采集纯人工 demo（30 条）

### 3.1 创建数据目录

```bash
mkdir -p "${LOCAL_DATA_ROOT}"
```

### 3.2 推荐采集方式：直接补到目标条数

你当前 [env.sh](/home/jy/Data/YCP/Evo-RL/env.sh) 里已经写了两个 helper：

- `show_demo_count`
- `record_to_total`

建议首轮 demo 采集优先这样用：

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*
record_to_total 30
```

这条命令的意思是：把当前 demo 数据集总数补到 `30` 条。

- 如果当前是 `0` 条，它就录 `30` 条
- 如果当前已经有 `10` 条，它就只再录 `20` 条
- 如果你中途退出，再执行一次同样命令，它会继续补到 `30`

如果以后想把 demo 从 `30` 扩充到 `50`，直接执行：

```bash
record_to_total 50
```

查看当前 demo 已经录了多少条：

```bash
show_demo_count
```

### 3.3 执行采集的底层命令

当前这套代码已经支持“手动逐条录制”模式。

- 不限制单条录制时长
- 不限制复位时长
- 由你手动按键结束当前条、开始下一条
- 适合你现在这种一条一条慢慢录、边看边确认的采集方式

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*

lerobot-record \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}" \
  --dataset.repo_id="${DEMO_REPO_ID}" \
  --dataset.root="${LOCAL_DATA_ROOT}/${DEMO_REPO_ID}" \
  --dataset.single_task="${TASK_TEXT}" \
  --dataset.num_episodes=30 \
  --dataset.episode_time_s=0 \
  --dataset.reset_time_s=0 \
  --dataset.push_to_hub=false \
  --display_data=true \
  ${SO_TELEOP_ARGS} \
  ${SO_FOLLOWER_TUNING_ARGS}
```

这里约定：

- `--dataset.episode_time_s=0` 表示当前条录制时间不限制，直到你手动结束
- `--dataset.reset_time_s=0` 表示保存后不自动复位，等你手动复位完成后，再按 `下方向键` 开始下一条
- 当前代码已经改成“保存后进入复位阶段提示，等待你手动复位并按 `下方向键` 继续”
- 终端会明确显示 `等待开始`、`正在录制`、`等待复位`、`采集完成` 这些阶段提示

### 3.4 单条 demo 测试

如果你想先跑一条，确认机械臂、相机、终端提示都正常，再正式录 30 条，可以先用：

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*

lerobot-record \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}" \
  --dataset.repo_id="local/${TASK_NAME}_demo_test2" \
  --dataset.root="${LOCAL_DATA_ROOT}/local/${TASK_NAME}_demo_test2" \
  --dataset.single_task="${TASK_TEXT}" \
  --dataset.num_episodes=1 \
  --dataset.episode_time_s=0 \
  --dataset.reset_time_s=0 \
  --dataset.push_to_hub=false \
  --display_data=true \
  ${SO_TELEOP_ARGS} \
  ${SO_FOLLOWER_TUNING_ARGS}
```

### 3.5 手动录制时的按键和终端行为

进入录制后，按键行为如下：

- `左方向键`：丢弃当前条并重录
- `下方向键`：开始当前条录制，或在复位完成后开始下一条
- `Esc`：停止整个采集会话
- `i`：切换人工接管
- `s`：成功并结束当前条
- `f`：失败并结束当前条

终端会给出类似提示：

- 当前是 `等待开始`、`正在录制` 还是 `等待复位`
- 当前正在录第几条
- 上一条是否已经保存
- 是否正在等待你按下 `下方向键` 开始下一条

建议采集流程：

1. 先手动准备好场景
2. 按下 `下方向键` 开始当前条
3. 完成一次完整示教
4. 按 `s` 或 `f` 结束当前条
5. 手动复位场景
6. 再按 `下方向键` 开始下一条

### 3.6 如果中途退出，不要删数据

重新执行同样流程即可：

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*
record_to_total 30
```

它会自动读取当前已经保存了多少条，然后继续补到 `30`。

### 3.7 采集要求

- 这 30 条是纯人工示范，不要加载 policy
- 尽量保证每条都完成“左臂抓方块 -> 递给右臂”
- 成功轨迹优先，失败样本不要太多
- 任务文本固定用 `${TASK_TEXT}`，不要在不同数据集里乱改

### 3.8 采完检查

```bash
show_demo_count
lerobot-dataset-report --dataset "${LOCAL_DATA_ROOT}/${DEMO_REPO_ID}"
```

通过标准：

- `episode` 数量至少 30
- 图像、状态、动作字段齐全
- 任务文本统一
- 每条 episode 长度大致合理

---

## Step 4：A100 训练初始 pi05 SFT

### 4.1 把 demo 数据传到 A100

如果你当前使用的是你给过信息的 H100 服务器：

- Host：`172.20.125.110`
- Port：`2201`
- User：`neu_lab1`
- H100 上 demo 目标目录：`/data2/ycp/lerobot_datasets/h100_server/local/bi_so101_handover_cube_demo`

你现在这台本机对应的 demo 数据目录是：

```bash
/home/jy/Data/YCP/lerobot_datasets/local/bi_so101_handover_cube_demo
```

直接执行下面两条命令即可上传：

```bash
ssh -p 2201 neu_lab1@172.20.125.110 "mkdir -p /data2/ycp/lerobot_datasets/h100_server/local/bi_so101_handover_cube_demo"
```

```bash
rsync -avh --progress -e "ssh -p 2201" \
  /home/jy/Data/YCP/lerobot_datasets/local/bi_so101_handover_cube_demo/ \
  neu_lab1@172.20.125.110:/data2/ycp/lerobot_datasets/h100_server/local/bi_so101_handover_cube_demo/
```

如果执行时远端报错 `rsync: command not found`，说明 H100 没装 `rsync`，这时改用 `scp`：

```bash
scp -P 2201 -r \
  /home/jy/Data/YCP/lerobot_datasets/local/bi_so101_handover_cube_demo \
  neu_lab1@172.20.125.110:/data2/ycp/lerobot_datasets/h100_server/local/
```

如果你不是传这台 H100，而是传别的训练服务器，再用下面这个通用写法。

先在服务器上创建目标目录：

```bash
ssh <A100_USER>@<A100_HOST> "mkdir -p '${SERVER_DATA_ROOT}/${DEMO_REPO_ID}'"
```

再从本地同步：

```bash
rsync -avz "${LOCAL_DATA_ROOT}/${DEMO_REPO_ID}/" \
  <A100_USER>@<A100_HOST>:"${SERVER_DATA_ROOT}/${DEMO_REPO_ID}/"
```

### 4.2 A100 上初始化环境

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
```

如果你在共享服务器上，还可能需要：

```bash
export HF_HOME=/llm_jzm/cache/huggingface/
export HF_ENDPOINT=https://hf-mirror.com
export HF_TOKEN=<YOUR_HF_TOKEN>
export WANDB_API_KEY=<YOUR_WANDB_API_KEY>
```

### 4.3 训练初始 pi05 policy

```bash
lerobot-train \
  --dataset.repo_id="${DEMO_REPO_ID}" \
  --dataset.root="${SERVER_DATA_ROOT}/${DEMO_REPO_ID}" \
  --policy.type=pi05 \
  --policy.pretrained_path=lerobot/pi05_base \
  --policy.device=cuda \
  --policy.dtype=bfloat16 \
  --policy.gradient_checkpointing=true \
  --policy.compile_model=false \
  --policy.freeze_vision_encoder=false \
  --policy.train_expert_only=false \
  --batch_size=8 \
  --steps=10000 \
  --acp.enable=false \
  --output_dir="outputs/train/${PI05_SFT_R0_NAME}" \
  --job_name="${PI05_SFT_R0_NAME}" \
  --wandb.enable=false \
  --policy.push_to_hub=false
```

如果你已经配置了 wandb，可以把 `--wandb.enable=false` 改成 `true`。

### 4.4 OOM 时怎么改

优先按这个顺序降：

1. `--batch_size=4`
2. 仍然爆显存再考虑降低 `steps`

### 4.5 训练完成后检查

```bash
ls "outputs/train/${PI05_SFT_R0_NAME}/checkpoints/last/pretrained_model"
```

通过标准：

- 存在 `config.json`
- 存在 `model.safetensors`

### 4.6 把初始 policy 拷回本地

先在本地创建目录：

```bash
mkdir -p /home/jy/Data/YCP/policies/"${PI05_SFT_R0_NAME}"
```

再拉回本地：

```bash
rsync -avz <A100_USER>@<A100_HOST>:"/data2/ycp/Evo-RL/outputs/train/${PI05_SFT_R0_NAME}/checkpoints/last/pretrained_model/" \
  /home/jy/Data/YCP/policies/"${PI05_SFT_R0_NAME}/"
```

---

## Step 5：本地 human-in-loop rollout（20 条 round1）

这一阶段不是纯人工示范，而是“policy 先做，人随时接管纠正”。

### 5.1 快捷键

| 按键 | 含义 |
| --- | --- |
| `i` | 开始/结束人工接管 |
| `s` | 标记成功并结束当前 episode |
| `f` | 标记失败并结束当前 episode |
| `→` | 提前结束当前轮 |
| `←` | 提前结束并重录当前 episode |
| `Esc` | 结束整个录制会话 |

### 5.2 执行 human-in-loop 采集

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*

mkdir -p "${LOCAL_DATA_ROOT}/${ROUND1_REPO_ID}"

lerobot-human-inloop-record \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}" \
  --dataset.repo_id="${ROUND1_REPO_ID}" \
  --dataset.root="${LOCAL_DATA_ROOT}/${ROUND1_REPO_ID}" \
  --dataset.single_task="${TASK_TEXT}" \
  --dataset.num_episodes=20 \
  --dataset.episode_time_s=60 \
  --dataset.reset_time_s=0 \
  --dataset.push_to_hub=false \
  --display_data=true \
  --policy.path=/home/jy/Data/YCP/policies/${PI05_SFT_R0_NAME} \
  ${SO_TELEOP_ARGS} \
  ${SO_FOLLOWER_TUNING_ARGS}
```

### 5.3 这一步怎么操作

- 先让 policy 自己执行
- 眼看要失败时按 `i` 接管
- 修正到安全状态后再按 `i` 交还 policy
- 每条 episode 结束时认真按 `s` 或 `f`
- 保存后进入手动复位阶段，复位完成后按 `下方向键` 开始下一条

第一次用带 policy 的 human-in-loop 录制时，程序会让你确认一次 reset pose，这是正常现象。

### 5.4 round1 采完检查

```bash
lerobot-dataset-report --dataset "${LOCAL_DATA_ROOT}/${ROUND1_REPO_ID}"
```

通过标准：

- episode 数量正确
- 有成功也有失败，不要全成功或全失败
- 数据集中包含：

```text
complementary_info.policy_action
complementary_info.is_intervention
complementary_info.state
complementary_info.collector_policy_id
episode_success
```

---

## Step 6：A100 合并 demo + round1

### 6.1 传 round1 到 A100

如果你当前使用的是你给过信息的 H100 服务器，那么 round1 对应的目标目录是：

```bash
/data2/ycp/lerobot_datasets/h100_server/local/eval_bi_so101_handover_cube_round1
```

等你本机采完 round1 后，直接执行：

```bash
ssh -p 2201 neu_lab1@172.20.125.110 "mkdir -p /data2/ycp/lerobot_datasets/h100_server/local/eval_bi_so101_handover_cube_round1"
```

```bash
rsync -avh --progress -e "ssh -p 2201" \
  /home/jy/Data/YCP/lerobot_datasets/local/eval_bi_so101_handover_cube_round1/ \
  neu_lab1@172.20.125.110:/data2/ycp/lerobot_datasets/h100_server/local/eval_bi_so101_handover_cube_round1/
```

如果执行时远端报错 `rsync: command not found`，说明 H100 没装 `rsync`，这时改用 `scp`：

```bash
scp -P 2201 -r \
  /home/jy/Data/YCP/lerobot_datasets/local/eval_bi_so101_handover_cube_round1 \
  neu_lab1@172.20.125.110:/data2/ycp/lerobot_datasets/h100_server/local/
```

如果你不是传这台 H100，而是传别的训练服务器，再用下面这个通用写法。

```bash
ssh <A100_USER>@<A100_HOST> "mkdir -p '${SERVER_DATA_ROOT}/${ROUND1_REPO_ID}'"
```

```bash
rsync -avz "${LOCAL_DATA_ROOT}/${ROUND1_REPO_ID}/" \
  <A100_USER>@<A100_HOST>:"${SERVER_DATA_ROOT}/${ROUND1_REPO_ID}/"
```

### 6.2 在 A100 上 merge

注意这里的 `--root` 是父目录，不是数据集目录本身。

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh

lerobot-edit-dataset \
  --repo_id="${MERGED_R1_REPO_ID}" \
  --root="${SERVER_DATA_ROOT}" \
  --operation.type=merge \
  --operation.repo_ids="['${DEMO_REPO_ID}','${ROUND1_REPO_ID}']"
```

### 6.3 检查 merge 结果

```bash
lerobot-dataset-report --dataset "${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID}"
```

通过标准：

- 总 episode 数 = demo + round1
- task 文本统一
- `episode_success` 还在
- intervention 相关字段还在

---

## Step 7：A100 训练 value model

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh

lerobot-value-train \
  --dataset.repo_id="${MERGED_R1_REPO_ID}" \
  --dataset.root="${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID}" \
  --value.type=pistar06 \
  --value.device=cuda \
  --value.dtype=bfloat16 \
  --targets.success_field=episode_success \
  --targets.default_success=failure \
  --targets.c_fail_coef=1.0 \
  --batch_size=64 \
  --steps=8000 \
  --output_dir="outputs/value_train/${VALUE_R1_NAME}" \
  --job_name="${VALUE_R1_NAME}" \
  --wandb.enable=false \
  --value.push_to_hub=false
```

如果爆显存，先改成：

```bash
--batch_size=32
```

训练完成后检查：

```bash
ls "outputs/value_train/${VALUE_R1_NAME}"
```

---

## Step 8：A100 做 value inference，写回 ACP indicator

这一步会把 value、advantage、indicator 写回合并后的数据集。

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh

lerobot-value-infer \
  --dataset.repo_id="${MERGED_R1_REPO_ID}" \
  --dataset.root="${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID}" \
  --inference.checkpoint_path="outputs/value_train/${VALUE_R1_NAME}" \
  --inference.checkpoint_ref=last \
  --runtime.device=cuda \
  --runtime.batch_size=128 \
  --acp.enable=true \
  --acp.n_step=50 \
  --acp.positive_ratio=0.3 \
  --acp.c_fail_coef=1.0 \
  --output_dir="outputs/value_infer/${VALUE_R1_NAME}" \
  --job_name="${VALUE_R1_NAME}_infer"
```

### 8.1 写回后检查

```bash
lerobot-dataset-report --dataset "${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID}"
```

至少要确认数据集中已经出现这些字段：

```text
complementary_info.value
complementary_info.advantage
complementary_info.acp_indicator
```

如果你未来改了 `value_field / advantage_field / indicator_field` 的名字，后面的 ACP 训练命令要一起改。

---

## Step 9：A100 训练 ACP policy

这里从初始 `pi05_sft_r0` 继续训一版带 ACP 条件的 policy。

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh

lerobot-train \
  --dataset.repo_id="${MERGED_R1_REPO_ID}" \
  --dataset.root="${SERVER_DATA_ROOT}/${MERGED_R1_REPO_ID}" \
  --policy.type=pi05 \
  --policy.pretrained_path="outputs/train/${PI05_SFT_R0_NAME}/checkpoints/last/pretrained_model" \
  --policy.device=cuda \
  --policy.dtype=bfloat16 \
  --policy.gradient_checkpointing=true \
  --policy.compile_model=false \
  --policy.freeze_vision_encoder=false \
  --policy.train_expert_only=false \
  --batch_size=8 \
  --steps=10000 \
  --acp.enable=true \
  --acp.indicator_field=complementary_info.acp_indicator \
  --acp.indicator_dropout_prob=0.3 \
  --output_dir="outputs/train/${PI05_ACP_R1_NAME}" \
  --job_name="${PI05_ACP_R1_NAME}" \
  --wandb.enable=false \
  --policy.push_to_hub=false
```

训练完成后检查：

```bash
ls "outputs/train/${PI05_ACP_R1_NAME}/checkpoints/last/pretrained_model"
```

---

## Step 10：本地部署 ACP policy，进入 round2

### 10.1 先把 ACP policy 拷回本地

```bash
mkdir -p /home/jy/Data/YCP/policies/"${PI05_ACP_R1_NAME}"
```

```bash
rsync -avz <A100_USER>@<A100_HOST>:"/data2/ycp/Evo-RL/outputs/train/${PI05_ACP_R1_NAME}/checkpoints/last/pretrained_model/" \
  /home/jy/Data/YCP/policies/"${PI05_ACP_R1_NAME}/"
```

### 10.2 本地用新 policy 做下一轮 human-in-loop

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
sudo chmod 666 /dev/ttyACM*

lerobot-human-inloop-record \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}" \
  --dataset.repo_id="local/eval_${TASK_NAME}_round2" \
  --dataset.root="${LOCAL_DATA_ROOT}/local/eval_${TASK_NAME}_round2" \
  --dataset.single_task="${TASK_TEXT}" \
  --dataset.num_episodes=20 \
  --dataset.episode_time_s=60 \
  --dataset.reset_time_s=0 \
  --dataset.push_to_hub=false \
  --display_data=true \
  --policy.path=/home/jy/Data/YCP/policies/${PI05_ACP_R1_NAME} \
  ${SO_TELEOP_ARGS} \
  ${SO_FOLLOWER_TUNING_ARGS}
```

从这里开始，就进入下一轮迭代了。

---

## 附录 A：本地联调最常用命令汇总

### A.1 每次开新终端

```bash
cd ~/Data/YCP/Evo-RL
conda activate evo-rl
source env.sh
```

### A.2 看串口

```bash
ls -la /dev/serial/by-id/
```

### A.3 看相机

```bash
ls -l /dev/v4l/by-path
v4l2-ctl --list-devices
```

### A.4 单独测试相机

```bash
ffplay -f v4l2 -input_format mjpeg -framerate 30 -video_size 640x480 "$LEFT_WRIST_CAM"
ffplay -f v4l2 -input_format mjpeg -framerate 30 -video_size 640x480 "$RIGHT_WRIST_CAM"
ffplay -f v4l2 -input_format mjpeg -framerate 30 -video_size 640x480 "$FRONT_CAM"
```

### A.5 双臂遥操作

```bash
lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}"
```

### A.6 双臂遥操作 + 三路相机显示

```bash
lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port="${LEFT_FOLLOWER_PORT}" \
  --robot.right_arm_config.port="${RIGHT_FOLLOWER_PORT}" \
  --robot.id="${ROBOT_ID}" \
  --robot.left_arm_config.cameras="${LEFT_CAM_CFG}" \
  --robot.right_arm_config.cameras="${RIGHT_CAM_CFG}" \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port="${LEFT_LEADER_PORT}" \
  --teleop.right_arm_config.port="${RIGHT_LEADER_PORT}" \
  --teleop.left_arm_config.gripper_input_min=0 \
  --teleop.left_arm_config.gripper_input_max=40 \
  --teleop.right_arm_config.gripper_input_min=0 \
  --teleop.right_arm_config.gripper_input_max=40 \
  --teleop.id="${TELEOP_ID}" \
  --display_data=true
```

---

## 附录 B：常见坑

### B.1 报没有校准文件

先检查：

```bash
ls ~/.cache/huggingface/lerobot/calibration/robots/so_follower/
ls ~/.cache/huggingface/lerobot/calibration/teleoperators/so_leader/
```

当前双臂配置下，文件名必须和这两个 ID 匹配：

- `ROBOT_ID=bi_follower_arm`
- `TELEOP_ID=bi_leader_arm`

### B.2 夹爪主臂动很多，从臂动很少

用当前文档里的夹爪映射参数：

```bash
--teleop.left_arm_config.gripper_input_min=0
--teleop.left_arm_config.gripper_input_max=40
--teleop.right_arm_config.gripper_input_min=0
--teleop.right_arm_config.gripper_input_max=40
```

### B.3 相机 `MJPG` 设置失败

如果日志类似：

```text
failed to set fourcc=MJPG (actual=YUYV, success=True)
```

只要画面正常，这不是致命错误，可以继续。

### B.4 OpenCV 回报 `fps=30` 设置失败

如果日志类似：

```text
failed to set fps=30 (actual_fps=120...)
```

当前代码已经放宽为告警模式。只要相机能稳定出图，一般可以继续。

### B.5 `lerobot-edit-dataset` 的 `--root` 和别的命令不一样

这个一定记住：

- `lerobot-record / human-inloop / train / value-train / value-infer` 里的 `dataset.root` 用数据集目录本身
- `lerobot-edit-dataset --root` 用数据集父目录

例子：

```bash
# 这是数据集目录本身
${LOCAL_DATA_ROOT}/${DEMO_REPO_ID}

# 这是父目录
${SERVER_DATA_ROOT}
```
